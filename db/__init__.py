"""
db package — SQLite database setup and ORM models (Stage 3).

Planned modules
---------------
db/database.py    — SQLAlchemy engine and session factory
db/orm_models.py  — SQLAlchemy ORM models (User, Subscription, Invoice)
db/seed.py        — Script to populate the database with dummy data
"""
